import { SET_AUTH, ERASE, SET_FINGERPRINT, SET_EMAIL, SET_ADDITIONAL } from './mutations.types'
import mutations from '@/app/store/mutations/mutations'

export default mutations
export { SET_AUTH, ERASE, SET_FINGERPRINT, SET_EMAIL, SET_ADDITIONAL }
